export interface MockType {
  id?: string;
  createdAt: string;
  name: string;
  image: string;
  category: string;
  alcoholic: string;
  instructions: string;
}

export interface MockState {
  id: number | null;
}
